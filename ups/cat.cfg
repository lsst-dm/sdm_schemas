# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["db", "daf_persistence"],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasSwigFiles=False,
)
